apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: saleor-media
  name: saleor-media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi

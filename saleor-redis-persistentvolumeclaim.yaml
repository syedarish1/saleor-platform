apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: saleor-redis
  name: saleor-redis
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
